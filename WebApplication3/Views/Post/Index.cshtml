@model IEnumerable<WebApplication3.Models.Entities.Post>

@{
    ViewBag.Title = "Index";
}

<center><img src="~/Content/image/13233486_1001721673256879_455551111_n.png" /></center>
<p>

</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DataPost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.content)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DataPost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.content)
        </td>
    </tr>
}

</table>
