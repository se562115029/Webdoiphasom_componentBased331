@model WebApplication3.Models.Entities.Payment

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm("Edit", "Payment", FormMethod.Post,
                                                                                                                    new { enctype = "multipart/form-data" }))
{

    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Payment</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.PaymentID)

        <<div class="form-group">
            @Html.LabelFor(model => model.imageTranferReceipt, htmlAttributes: new { @class = "control-label col-md-2" })
            <input type="file" name="file" id="file" style="width: 100%;" /> <br />
       </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
